#
# This directory contains only generated files.
# The files are generated in their specified directories,
# so if you want the documentation, run "omake tex" in the
# MetaPRL root directory.
#

#
# Clean up
#
clean:
	$(CLEAN)