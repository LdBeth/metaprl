# C preprocessing
OCAMLFLAGS = $(OCAMLFLAGS) -pp "$(CAMLP4MACRO)"

OCAMLINCLUDES +=\
	-I $(LIBMOJAVE)/stdlib\
	-I $(LIBMOJAVE)/util\
	-I $(LIBMOJAVE)/unix

include Files

FILES = $(MLLIB_FILES)

OCamlLibraryInstall($(MPINSTALL), $(LIB), util, $(FILES))

#
# Clean up
#
clean:
    $(CLEAN)
