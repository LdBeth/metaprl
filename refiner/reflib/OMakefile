OCAMLPPFLAGS =
OCAMLFLAGS += -rectypes

OCAMLINCLUDES[] +=
    $(CAMLP4LIB)
    ../refbase
    ../refsig
    ../refiner
    ../term_ds
    ../term_gen

OCamlLibraryInstall(all, $(LIB), reflib, $(REFLIB_FILES))

clean:
    $(CLEAN)
