OCAMLINCLUDES[] +=
    ../refbase
    ../refsig
    ../term_std
    ../term_gen
    ../term_ds
    ../rewrite

OCamlLibraryInstall(all, $(LIB), refine, $(REFINER_FILES))

#
# Clean up
#
clean:
    $(CLEAN)
