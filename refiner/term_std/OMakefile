CAMLP4INCLUDES = ../refsig

OCAMLINCLUDES[] +=
	../refbase
	../refsig
	../term_gen

OCAMLFLAGS += -rectypes

OCamlLibraryInstall(all, $(LIB), termstd, $(TERM_STD_FILES))

MLZFILES = term_std_sig

clean:
    $(CLEAN)
