(*
 * Match against a redex term.
 *)

module type RewriteMatchRedexSig =
sig
   type term
   type address
   type stack
   type rwterm

   val match_redex : address array -> stack array -> term -> term list -> rwterm list -> unit
end

(*
 * -*-
 * Local Variables:
 * Caml-master: "refiner"
 * End:
 * -*-
 *)
