#
# Name of this library
#
LIBNAME = trefiner

LMFILES = thread_refiner

OCAMLFLAGS += -rectypes

OCamlLibraryInstall($(MPINSTALL), $(LIB), $(LIBNAME), $(LMFILES))

#
# Clean up
#
clean:
    $(CLEAN)
